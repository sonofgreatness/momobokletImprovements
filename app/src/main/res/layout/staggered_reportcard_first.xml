<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="@dimen/staggered_report_grid_card_width_portrait"
    android:layout_height="wrap_content"
    app:cardBackgroundColor="@android:color/transparent"
    app:cardElevation="0dp"
    app:cardPreventCornerOverlap="true"
    app:contentPaddingTop="@dimen/staggered_report_grid_margin_top_third">

    <!-- Product card layout used in MDC-103 and onward -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/report_image"
            android:layout_width="match_parent"
            android:layout_height="@dimen/staggered_report_grid_card_height_portrait"
            android:background="@android:color/transparent"
             />


        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:id="@+id/report_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/black"
                android:text="@string/report_title"
                android:textAlignment="center"
                android:textAppearance="?attr/textAppearanceSubtitle2" />

            <RelativeLayout
                android:id="@+id/report_buttons_relativelayout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                >

                <ImageButton
                    android:id="@+id/report_delete_btn"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_alignParentStart="true"
                    android:src="@drawable/delete_icon"
                    android:contentDescription="@string/delete_report_button"
                    />
                <ImageButton
                    android:id="@+id/report_share_btn"
                    android:layout_height="48dp"
                    android:layout_width="48dp"
                    android:src="@drawable/share_icon"
                    android:layout_alignParentEnd="true"
                    android:contentDescription="@string/share_report_button"
                    />
            </RelativeLayout>
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
